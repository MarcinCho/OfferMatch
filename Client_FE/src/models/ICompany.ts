export interface ICompany {
  id: number;
  companyName: string;
  companyType: string;
  description: string;
  phoneNumber: string;
  email: string;
  address: string;
  contactPerson: string; // in the future, it will be user interface
}
