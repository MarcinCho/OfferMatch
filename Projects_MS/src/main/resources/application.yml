spring:
  application:
    name: projects
  data:
    mongodb:
      host: "localhost"
      port: 27017
      database: "service_match"
      user: smatch
      password: servicematch
  cloud:
    config:
      enabled: false
    bus:
      enabled: false
